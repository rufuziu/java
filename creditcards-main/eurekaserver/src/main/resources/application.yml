spring:
  application:
    name: eurekaserver
  security:
    user:
      name: admin
      password: admin

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false